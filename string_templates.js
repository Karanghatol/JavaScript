{

    //this (``) is used
    const name = "karan ghatol"
    const age = 19

    // ${} to directly include variables in to string
    console.log(`name : ${name} \nage : ${age}`);

    //to write multiline string
    let str = `
        this 
        is
        multiline 
        string
    `
    console.log(str);

}